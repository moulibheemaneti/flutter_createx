name: flutter_createx
description: An interactive terminal wizard for flutter create. Browse and configure all flags visually, then execute with approval.
version: 1.0.0
repository: https://github.com/moulibheemaneti/flutter_createx

environment:
  sdk: ">=3.12.0"

# Add regular dependencies here.
dependencies:
  dart_console: ^5.0.0
  process_run: ^1.3.3+1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  dart_husky: ^1.2.1

executables:
  flutter_createx: main
